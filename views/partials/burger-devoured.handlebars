<div class="col-11 pr-0">
    <input type="text" class="form-control rounded-0 text-left" value="{{id}}. {{burger_name}}" disabled>
</div>
<div class="col-1 pl-0">
    <button class='btn btn-danger border delete-burger' data-id="{{id}}">x</button>
</div>